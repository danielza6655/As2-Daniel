import { MyAttributeDirective } from './my-attribute.directive';

describe('MyAttributeDirective', () => {
  it('should create an instance', () => {
    const directive = new MyAttributeDirective();
    expect(directive).toBeTruthy();
  });
});
